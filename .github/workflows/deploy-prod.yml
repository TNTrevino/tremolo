name: Deploy Prod - Pi

on:
  push:
    branches: [prod]
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      runner: geekom
      frontend-dest: reverse-proxy:/var/www/tremolo/
